🤖 Basic AI Chatbot
This is a simple Python-based AI chatbot that responds to basic user inputs. The chatbot uses a rule-based approach with predefined responses stored in a dictionary. It continues responding until the user types "Bye".

🚀 Features
Responds to common user messages
Uses a simple dictionary-based response system
Easy to understand and customize
Gracefully exits when the user types "Bye"
